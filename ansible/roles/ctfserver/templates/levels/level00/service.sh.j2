{% include "levels/base_service.sh.j2" %}

# Level00 spesific service code

# Run the emulated browser that is going to be hacked.

#### NOTE: This is meant for session services

# We're for now living on "temporary files" for the start/stop control of session services.
# As long as the temporary file exists, the service will run as well.

# Example ish;

#while ! [ -f /tmp/list.txt ];
#do
#    echo "#"
#    sleep 1
#done


while true;
do
  /usr/bin/phantomjs {{ ctfchroot_datadir }}/libexec/levels/{{ item.user }}/xss-bot.js  > /dev/null 2>&1;
done

